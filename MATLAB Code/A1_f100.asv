clear; clc; close all;

%% ==== 参数 ====
phi = 0; % 相位
n = 2; % 测试n个周期
M = 16; % 测试n个周期 M > n * (fs/f) = 2*n


A_list = [1, 2, 3];
f_list = [100, 500, 1000];

for k = 1:3
    A = A_list(k);
    f = f_list(k);

    fs = 2*f; % 采样率为两倍

    T = n*f; % 持续n个周期



